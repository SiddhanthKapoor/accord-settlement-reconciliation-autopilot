# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# Secrets
.env
backend/.env

# Local database (WAL mode leaves sidecar files)
backend/data/*.db
backend/data/*.db-*

# Generated datasets and reports. Reproducible from a seed; the frozen
# evaluation sets under backend/evaluations/ are committed deliberately,
# because the generator changes and a seed alone stops reproducing them.
backend/data/datasets/
backend/data/datasets_v2/
backend/data/eval_reports/

# Frontend
node_modules/
frontend/dist/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.log
